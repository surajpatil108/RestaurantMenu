Django>=5.0,<6.0
Pillow>=10.0
psycopg2-binary>=2.9  # PostgreSQL adapter (optional, swap for mysqlclient if needed)
gunicorn>=22.0  # Production WSGI server
whitenoise>=6.6  # Static/media files in production
python-dotenv>=1.0  # Environment variables
django-environ>=0.11  # Settings from .env (optional but recommended)